<%= form_for [@user, Item.new] do |f| %>
    <%= f.text_field :name, class: 'form-control', placeholder: "Item to add to list", autofocus: true %>
    <br>
    <%= f.submit "Add item", class: 'btn' %>
<% end %>
